# Codex 工作规范

## 项目目标

本仓库用于长期迭代开发 AI 智能求职招聘平台。项目采用前后端分离结构：

- `backend/`：Spring Boot 3 后端服务
- `frontend/`：Vue 3 前端应用
- `docs/`：路线图、每日计划、数据库脚本和部署文档

## 每日开发边界

- 只执行 `docs/DAILY_TASKS.md` 中当天任务。
- 不提前实现后续天数的功能。
- 每天完成一个小模块，保持提交粒度清晰。
- 每次提交都应可运行、可测试、可回滚。
- 不生成伪代码。
- 不留下空实现。
- 不引入与当天任务无关的重构。

## 代码规范

- 后端使用 Spring Boot 3 + Maven + MyBatis-Plus。
- 前端使用 Vue 3 + Vite + Element Plus。
- 后端接口使用统一返回格式。
- 命名保持清晰、直接、可维护。
- 数据库以 MySQL 8 为目标版本。
- 缓存以 Redis 为目标实现。
- 认证使用 JWT。

## 每次任务完成清单

1. 更新 `README.md`，说明当前进度、运行方式和测试方式。
2. 更新 `docs/DAILY_TASKS.md`，标记当天任务状态并记录完成内容。
3. 运行当前阶段可用的构建或测试命令。
4. 提交一个清晰的 Git commit。
5. 如果仓库已连接 GitHub，创建 Pull Request。

## Day 1 限制

Day 1 只允许初始化目录和文档，不实现后端、前端或数据库业务功能。

## Day 2 指引

Day 2 只搭建 Spring Boot 后端基础框架，保证后端项目可以构建或启动，不实现注册登录和业务接口。
